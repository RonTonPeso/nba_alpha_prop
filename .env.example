# Copy to `.env` and fill in real values. `.env` is gitignored.

# Anthropic API key for LLM calls (injury severity classification, etc.).
# Get one at https://console.anthropic.com. Set a budget cap there too.
ANTHROPIC_API_KEY=
